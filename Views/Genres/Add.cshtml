@{
    ViewData["Title"] = "Add Genre";
}
<h2>New Genre</h2>
<form asp-action="Add" method="post">
    <label>Genre Name: <input id="genreName" name="genreName" minlength="3" maxlength="20" /></label>
    <button type="submit">Submit</button>
</form>